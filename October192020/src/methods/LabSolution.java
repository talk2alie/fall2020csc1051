package methods;

/**
 * LabSolution
 */
public class LabSolution {
    public static void main(String[] args) {
        double sum = sum(5, 5.0);
        System.out.println(sum);

        String fullName;
    }
    
    // => sum int, double

    private static double sum(double first, double second) {
        return first + second;
    }

    private static int sum(int first, int second) {
        return first + second;


    }

    private static double sum(int first, double second) {
        return first + second;
    }

    private void setName(String name) {}

    private static String getFullName(String firstName, String lastName) {
        return firstName + " " + lastName;
    }

    private void concatenate(String text, char firstLetter, int randomNumber) {}

    private void concatenate(String text, char firstLetter) {
    }

    // concatenate String, char
    // concatenate String, char, int

    // a. the method's name
    // b. the data type of each of the methods parameters

    // getFullName String, String
}